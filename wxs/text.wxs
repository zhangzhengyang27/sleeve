/**
 * @作者 7七月
 * @微信公号 林间有风
 * @开源项目 $ http://7yue.pro
 * @免费专栏 $ http://course.7yue.pro
 * @我的课程 $ http://imooc.com/t/4294850
 * @创建时间 2019-09-15 02:23
 */

var lineBreak = function (text) {
    if (!text) {
        return
    }
    var reg = getRegExp('\\\\n', 'g')
    var t = text.replace(reg, '\n');
    return t
}

module.exports = {
    lineBreak: lineBreak
}